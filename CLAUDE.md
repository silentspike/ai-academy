The working rules for this repository live in [AGENTS.md](AGENTS.md).

That file is the single source of truth for principles, layout, commands,
critical rules, the workflow for a change, the evidence requirement, secret
hygiene, versioning and commit conventions. It applies to every agent and every
contributor; this file exists only so that tools looking for `CLAUDE.md` find it.
